'use client';
import {useState} from 'react';
import {signIn,signUp} from '../lib/auth';
export default function AuthPanel({onDone}:{onDone:()=>void}){
 const [mode,setMode]=useState<'signin'|'signup'>('signin'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 const submit=async(e:any)=>{e.preventDefault();setError('');setBusy(true);try{if(mode==='signin')await signIn(email,password);else{const d=await signUp(email,password,name);if(!d.session){setError('Account created. Check your email if email confirmation is enabled, then sign in.');return;}}onDone()}catch(x:any){setError(x.message||'Authentication failed')}finally{setBusy(false)}};
 return <div className="modal-overlay"><form className="modal auth-modal" onSubmit={submit}><h2>{mode==='signin'?'Member Sign In':'Create Member Account'}</h2><p className="page-subtitle">The shared community version uses Supabase authentication. Your private profile fields are protected by database policies.</p>{mode==='signup'&&<div className="field"><label>Name</label><input className="text-input" value={name} onChange={e=>setName(e.target.value)} required/></div>}<div className="field"><label>Email</label><input className="text-input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input className="text-input" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div>{error&&<div className="notice">{error}</div>}<div className="form-actions"><button type="button" className="btn" onClick={()=>setMode(mode==='signin'?'signup':'signin')}>{mode==='signin'?'Create account':'Back to sign in'}</button><button className="btn primary" disabled={busy}>{busy?'Please wait…':mode==='signin'?'Sign In':'Create Account'}</button></div></form></div>
}
