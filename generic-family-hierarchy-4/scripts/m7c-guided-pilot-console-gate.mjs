import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');let passed=0;const ok=(n,c)=>{if(!c){console.error(`FAIL: ${n}`);process.exitCode=1}else{passed++;console.log(`PASS: ${n}`)}};
const ui=read('components/AdminPilotLaunchConsole.tsx'),home=read('components/MyNetworksHome.tsx'),sql=read('supabase/migrations/063_m7c_guided_pilot_admin_console.sql'),remote=read('capabilities/pilot-console/remote.ts'),docs=read('MISSION-7C-GUIDED-PILOT-ADMIN-CONSOLE.md'),pkg=JSON.parse(read('package.json'));
ok('Pilot console component exists',ui.includes('guided-pilot-console')&&ui.includes('M7CHighestLeverageTxt'));ok('My Networks includes pilot console',home.includes('<AdminPilotLaunchConsole'));ok('Portfolio is admin scoped',sql.includes("nm.role in ('owner','admin')")&&sql.includes("nm.user_id=auth.uid()"));ok('Portfolio snapshot remains aggregate only',sql.includes('count(*)')&&!sql.includes('search_query')&&!sql.includes('target_entity_id'));ok('30-day pilot outcomes are measured',sql.includes("interval '30 days'")&&sql.includes("'introduction_accepted'"));ok('Deterministic attention logic exists',remote.includes('age>=14')&&remote.includes('health="attention"'));ok('Highest-priority network is deterministic',remote.includes('networks.sort')&&remote.includes('highestPriorityNetworkId'));ok('M7-C mission is documented',docs.includes('Guided Pilot')&&docs.includes('highest-leverage'));ok('Validation command exists',pkg.scripts?.['validate:m7c']?.includes('m7c-guided-pilot-console-gate.mjs'));console.log(`M7-C source gate: ${passed}/9 passed`);if(process.exitCode)process.exit(process.exitCode);
