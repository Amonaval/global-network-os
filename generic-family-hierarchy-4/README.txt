Run APPLY-FIX.ps1 FROM THE PROJECT ROOT.

It forcibly overwrites:
  components/FamilySignatureExperience.tsx

Verified replacements:
  yourFamilyTogether -> FamTogetherTitleTxt
  people             -> PeopleTxt
  addRelative        -> AddRelativeTxt

The script fails if any obsolete token remains after copy.
