import type {Page,TestInfo} from '@playwright/test';
import fs from 'node:fs';import path from 'node:path';
export type RuntimeIssue={type:'console'|'pageerror'|'requestfailed'|'http'|'ui';message:string;url?:string;status?:number;test:string;at:string};
const noise=[/favicon/i,/ResizeObserver loop/i];
export function attachRuntimeWatch(page:Page,testInfo:TestInfo){const issues:RuntimeIssue[]=[];const push=(x:Omit<RuntimeIssue,'test'|'at'>)=>{if(noise.some(r=>r.test(x.message)))return;issues.push({...x,test:testInfo.titlePath.join(' > '),at:new Date().toISOString()})};page.on('console',m=>{if(m.type()==='error')push({type:'console',message:m.text()})});page.on('pageerror',e=>push({type:'pageerror',message:e.message}));page.on('requestfailed',r=>push({type:'requestfailed',message:r.failure()?.errorText||'request failed',url:r.url()}));page.on('response',r=>{if(r.status()>=500)push({type:'http',message:`HTTP ${r.status()} ${r.request().method()}`,status:r.status(),url:r.url()})});return {issues,flush(){if(!issues.length)return;const dir='qa-results/issues';fs.mkdirSync(dir,{recursive:true});fs.appendFileSync(path.join(dir,'runtime.ndjson'),issues.map(x=>JSON.stringify(x)).join('\n')+'\n')}}}
