import {expect,type Page} from '@playwright/test';
const keys={owner:['QA_OWNER_EMAIL','QA_OWNER_PASSWORD'],admin:['QA_ADMIN_EMAIL','QA_ADMIN_PASSWORD'],member:['QA_MEMBER_EMAIL','QA_MEMBER_PASSWORD'],invitee:['QA_INVITEE_EMAIL','QA_INVITEE_PASSWORD']} as const;
export async function login(page:Page,role:keyof typeof keys){const [ek,pk]=keys[role],email=process.env[ek],password=process.env[pk];if(!email||!password)throw new Error(`Missing ${ek}/${pk} in .env.qa`);await page.goto('/');const emailBox=page.getByRole('textbox',{name:/email/i}).first();if(await emailBox.count()){await emailBox.fill(email);const pass=page.locator('input[type=password]').first();await pass.fill(password);await page.getByRole('button',{name:/sign in|login/i}).first().click();await expect(page.locator('body')).not.toContainText(/invalid login|invalid credentials/i,{timeout:15000})}}
