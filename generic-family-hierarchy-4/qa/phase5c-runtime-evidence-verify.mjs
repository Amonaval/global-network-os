import fs from 'node:fs';const r='qa-results/PHASE5C-RUNTIME-EVIDENCE.json',b='qa-results/db/PHASE5B-FRESH-MIGRATION-REPLAY.json';const failures=[];if(!fs.existsSync(r))failures.push(`missing ${r}`);if(!fs.existsSync(b))failures.push(`missing ${b}`);if(!failures.length){const re=JSON.parse(fs.readFileSync(r,'utf8')),be=JSON.parse(fs.readFileSync(b,'utf8'));if(re.status!=='passed')failures.push(`runtime evidence status ${re.status}`);if(be.status!=='passed')failures.push(`Phase-5B replay status ${be.status}`);if(re.projectRef!==be.projectRef)failures.push('runtime project differs from Phase-5B replay project');if(re.phase5bSourceFingerprint!==be.sourceFingerprint)failures.push('runtime evidence is bound to a different migration source fingerprint');const cleanup=(re.steps||[]).find(x=>x.name==='release-fixture-cleanup');if(!cleanup||cleanup.status!=='passed')failures.push('release fixture cleanup did not pass')}if(failures.length){console.error(`Phase-5C runtime evidence verification FAILED: ${failures.join('; ')}`);process.exit(1)}console.log('Phase-5C runtime evidence verification PASS');
