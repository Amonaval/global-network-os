import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const spec=fs.readFileSync('qa/e2e/19-phase4b-data-integrity-recovery.spec.ts','utf8');const runner=fs.readFileSync('qa/run-phase4b-certification.mjs','utf8');
test('Phase 4B browser suite contains no service-role or destructive lifecycle operations',()=>{for(const token of ['SUPABASE_SERVICE_ROLE_KEY','/purge','delete_owned_network_permanently','archive_owned_network','restore_owned_network','storage.from('])assert.equal(spec.includes(token),false,`forbidden Phase-4B browser token: ${token}`)});
test('Phase 4B valid/malformed import browser proofs never click commit',()=>{assert.equal(/qa-guided-import-commit[^\n]*\.click\(/.test(spec),false,'Phase 4B may inspect commit readiness but must not execute import mutation')});
test('Phase 4B certification runner does not invoke migration/RLS/RPC/storage/seed/cleanup/destructive suites',()=>{for(const token of ['migration-replay.mjs','rls-adversarial.mjs','rpc-permission-audit.mjs','rpc-smoke.mjs','qa/setup/seed.mjs','qa/setup/cleanup.mjs','70-destructive-lifecycle-import.spec.ts'])assert.equal(runner.toLowerCase().includes(token),false,`forbidden Phase-4B runner invocation: ${token}`)});
test('Phase 4B explicitly includes local all-nine data contracts and headed single-worker browser recovery suite',()=>{assert.ok(runner.includes('qa:phase4b:local'));assert.ok(runner.includes('19-phase4b-data-integrity-recovery.spec.ts'));assert.ok(runner.includes("'--workers=1'"));assert.ok(runner.includes("'--headed'"))});
