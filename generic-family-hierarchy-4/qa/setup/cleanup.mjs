import fs from 'node:fs';import {assertMutationAllowed,loadQaEnv} from '../runtime/env.mjs';import {serviceClient} from '../runtime/supabase.mjs';
loadQaEnv();assertMutationAllowed();const file='qa-results/fixtures/seed-state.json';if(!fs.existsSync(file)){console.log('No seed state; nothing to clean.');process.exit(0)}const state=JSON.parse(fs.readFileSync(file,'utf8'));const service=serviceClient();const ids=[...Object.values(state.networks||{}).map(x=>x.id),state.tenantB?.id].filter(Boolean);for(const id of ids){const {error}=await service.from('networks').delete().eq('id',id);if(error)throw new Error(`Cleanup ${id}: ${error.message}`)}console.log(`QA cleanup removed ${ids.length} disposable networks. Dedicated QA auth users were preserved for reuse.`);
