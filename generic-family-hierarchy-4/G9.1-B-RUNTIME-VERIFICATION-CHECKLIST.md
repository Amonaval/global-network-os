# G9.1-B Runtime Verification Checklist

1. Apply migrations through `051_g9_1b_organization_knowledge_bootstrap.sql`.
2. Sign in as an Organization network owner/admin and open Admin. Confirm **G9.1-B Knowledge Discovery** appears.
3. Confirm the same inbox does not appear in Family, Alumni, Franchise or Business Trust Admin.
4. Submit evidence through `submit_organization_knowledge_evidence`; verify its source/evidence rows use the active `network_id`.
5. Submit candidate assertions using evidence IDs from the active network. Cross-network or evidence-free candidates must not be accepted.
6. Verify an expertise candidate: the accepted skill is committed through the existing `skill` affiliation model.
7. Verify an ownership/dependency candidate: the accepted fact is committed to `network_entity_relationships` with evidence/assertion metadata.
8. Verify an architectural-decision candidate: it becomes verified evidence but does not invent a graph relationship.
9. Reject a candidate and confirm the candidate + decision history remain visible in storage while canonical graph truth stays unchanged.
10. Re-run `npm run validate:g9.1b`; all G9/G8 chained regression gates must pass.
