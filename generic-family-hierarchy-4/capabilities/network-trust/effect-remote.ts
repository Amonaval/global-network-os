import {supabase} from "../../lib/supabase";import type {NetworkEffectPulse} from "../../core/trust/network-effect";function required(){if(!supabase)throw new Error("Shared Supabase mode is required.");return supabase}export async function fetchMyNetworkEffectPulse(days=30):Promise<NetworkEffectPulse>{const {data,error}=await required().rpc("get_my_network_effect_pulse",{p_days:days});if(error)throw error;return data as NetworkEffectPulse}
