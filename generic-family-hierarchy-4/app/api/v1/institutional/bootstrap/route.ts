import {bootstrapInstitution} from "../../../../../server/institutional/service";import {createRequestContext} from "../../../../../server/shared/request-context";import {commandFailure,commandSuccess,logCommand} from "../../../../../server/shared/response";import {objectBody,text} from "../../../../../server/shared/validation";import {CommandError} from "../../../../../server/shared/errors";export const runtime="nodejs";
export async function POST(request:Request){let requestId=request.headers.get("x-request-id")||crypto.randomUUID(),ctx:Awaited<ReturnType<typeof createRequestContext>>|null=null;try{ctx=await createRequestContext(request);requestId=ctx.requestId;const b=objectBody(await request.json());if(!Array.isArray(b.rows)||b.rows.length<1||b.rows.length>500)throw new CommandError("INVALID_INPUT","Bootstrap requires 1 to 500 rows.");const rows=b.rows.map((raw,i)=>{const r=objectBody(raw);return {kind:typeof r.kind==="string"?r.kind:undefined,label:text(r.label,`rows[${i}].label`,200),metadata:r.metadata&&typeof r.metadata==="object"&&!Array.isArray(r.metadata)?r.metadata as Record<string,unknown>:undefined,affiliations:r.affiliations&&typeof r.affiliations==="object"&&!Array.isArray(r.affiliations)?r.affiliations as Record<string,string|string[]>:undefined}});const data=await bootstrapInstitution(ctx,{rows});logCommand({requestId,actorId:ctx.user.id,command:"bootstrapInstitution",networkId:ctx.activeNetworkId,outcome:"success",startedAt:ctx.startedAt});return commandSuccess(requestId,data)}catch(e){if(ctx)logCommand({requestId,actorId:ctx.user.id,command:"bootstrapInstitution",networkId:ctx.activeNetworkId,outcome:"failure",startedAt:ctx.startedAt});return commandFailure(requestId,e)}}
