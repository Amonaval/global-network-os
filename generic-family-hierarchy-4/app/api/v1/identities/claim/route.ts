import {claimIdentity} from "../../../../../server/identity/service";import {createRequestContext} from "../../../../../server/shared/request-context";import {commandFailure,commandSuccess,logCommand} from "../../../../../server/shared/response";import {objectBody,text} from "../../../../../server/shared/validation";import {CommandError} from "../../../../../server/shared/errors";export const runtime="nodejs";
export async function POST(request:Request){let requestId=request.headers.get("x-request-id")||crypto.randomUUID(),ctx:Awaited<ReturnType<typeof createRequestContext>>|null=null;try{ctx=await createRequestContext(request);requestId=ctx.requestId;const b=objectBody(await request.json()),kind=text(b.kind,"kind",40);if(kind!=="family"&&kind!=="alumni"&&kind!=="productized")throw new CommandError("INVALID_NETWORK_KIND","Unsupported identity type.");const data=await claimIdentity(ctx,{kind,subjectId:text(b.subjectId,"subjectId",100)});logCommand({requestId,actorId:ctx.user.id,command:"claimIdentity",networkId:data.networkId,outcome:"success",startedAt:ctx.startedAt});return commandSuccess(requestId,data)}catch(e){if(ctx)logCommand({requestId,actorId:ctx.user.id,command:"claimIdentity",outcome:"failure",startedAt:ctx.startedAt});return commandFailure(requestId,e)}}
