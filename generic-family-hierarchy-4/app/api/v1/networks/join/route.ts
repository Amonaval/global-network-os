import {joinNetwork} from "../../../../../server/network/service";import {createRequestContext} from "../../../../../server/shared/request-context";import {commandFailure,commandSuccess,logCommand} from "../../../../../server/shared/response";import {objectBody,text} from "../../../../../server/shared/validation";import {CommandError} from "../../../../../server/shared/errors";export const runtime="nodejs";
export async function POST(request:Request){let requestId=request.headers.get("x-request-id")||crypto.randomUUID(),ctx:Awaited<ReturnType<typeof createRequestContext>>|null=null;try{ctx=await createRequestContext(request);requestId=ctx.requestId;const b=objectBody(await request.json()),kind=text(b.kind,"kind",20);if(kind!=="family"&&kind!=="productized")throw new CommandError("INVALID_NETWORK_KIND","Unsupported network type.");const data=await joinNetwork(ctx,{kind,code:text(b.code,"code",20).toUpperCase()});logCommand({requestId,actorId:ctx.user.id,command:"joinNetwork",networkId:data.networkId,outcome:"success",startedAt:ctx.startedAt});return commandSuccess(requestId,data)}catch(e){if(ctx)logCommand({requestId,actorId:ctx.user.id,command:"joinNetwork",outcome:"failure",startedAt:ctx.startedAt});return commandFailure(requestId,e)}}
