import {createGraphRelationship} from "../../../../../server/graph/service";import {createRequestContext} from "../../../../../server/shared/request-context";import {commandFailure,commandSuccess,logCommand} from "../../../../../server/shared/response";import {objectBody,text} from "../../../../../server/shared/validation";export const runtime="nodejs";
export async function POST(request:Request){let requestId=request.headers.get("x-request-id")||crypto.randomUUID(),ctx:Awaited<ReturnType<typeof createRequestContext>>|null=null;try{ctx=await createRequestContext(request);requestId=ctx.requestId;const b=objectBody(await request.json());const metadata=b.metadata&&typeof b.metadata==="object"&&!Array.isArray(b.metadata)?b.metadata as Record<string,unknown>:{};const data=await createGraphRelationship(ctx,{fromEntityId:text(b.fromEntityId,"fromEntityId",100),toEntityId:text(b.toEntityId,"toEntityId",100),relationshipType:text(b.relationshipType,"relationshipType",80),metadata});logCommand({requestId,actorId:ctx.user.id,command:"createGraphRelationship",networkId:ctx.activeNetworkId,outcome:"success",startedAt:ctx.startedAt});return commandSuccess(requestId,data,201)}catch(e){if(ctx)logCommand({requestId,actorId:ctx.user.id,command:"createGraphRelationship",networkId:ctx.activeNetworkId,outcome:"failure",startedAt:ctx.startedAt});return commandFailure(requestId,e)}}
